import { ContractTransformer } from './contract-transformer';

describe('ContractTransformer', () => {
  it('should create an instance', () => {
    expect(new ContractTransformer()).toBeTruthy();
  });
});
