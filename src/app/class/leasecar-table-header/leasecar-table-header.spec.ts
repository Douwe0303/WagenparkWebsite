import { LeasecarTableHeader } from './leasecar-table-header';

describe('LeasecarTableHeader', () => {
  it('should create an instance', () => {
    expect(new LeasecarTableHeader()).toBeTruthy();
  });
});
